# Frontend Environment Variables

# API Configuration
# For local development, leave empty to use http://localhost:8000
# For production, set to your API base URL or leave empty for same-domain deployment
NEXT_PUBLIC_API_URL=

# Fireworks App Configuration
NEXT_PUBLIC_FIREWORKS_APP_URL=

# Backend Environment Variables (for Vercel serverless functions)
# Note: Server no longer requires a Fireworks API key since clients provide their own
# FIREWORKS_API_KEY=  # No longer required - clients provide their own API keys
